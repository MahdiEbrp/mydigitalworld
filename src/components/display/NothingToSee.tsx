import Card, { CardContent, CardTitle } from '../Card';

const NothingToSee = () => {
    return (
        <Card>
            <CardTitle title='hmm..' />
            <CardContent>
                <p>
                    Well, this page may be empty, but at least it&apos;s not as empty as my wallet.😜
                </p>
            </CardContent>
        </Card>
    );
};

export default NothingToSee;